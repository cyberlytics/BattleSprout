{
    "name": "battlesprout",
    "scripts": {
        "install": "npm run install-backend & npm run install-frontend",
        "install-backend": "cd sys-src && cd backend && npm install",
        "install-frontend": "cd sys-src && cd frontend && npm install",
        "start": "npm run start-backend & npm run start-frontend",
        "start-backend": "cd sys-src && cd backend && npm run dev",
        "start-frontend": "cd sys-src && cd frontend && npm start"
    }
}
